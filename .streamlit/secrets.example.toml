# Example Streamlit secrets (do NOT store real secrets here)
# Copy this file to `.streamlit/secrets.toml` locally if needed, but never commit real secrets.

API_BASE = "https://electra-lens.vercel.app"
# DATABASE_URL = "postgresql://<DB_USER>:<DB_PASSWORD>@<HOST>:5432/<DB_NAME>"

# Add any other secret keys below as needed
# SECRET_KEY = "replace-with-your-secret"